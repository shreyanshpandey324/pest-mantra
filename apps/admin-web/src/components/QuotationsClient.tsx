"use client";import Link from"next/link";import{useMemo,useState}from"react";import{Quotation,QuotationStatus,QUOTATION_STATUS_LABELS}from"@/types/quotation";import{SERVICE_TYPE_LABELS}from"@/types/project";import{ui}from"@/lib/ui-classes";
const money=(n:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(n);const badge=(s:QuotationStatus)=>s==="accepted"?"text-success border-success/30 bg-success/10":s==="rejected"?"text-danger border-danger/30 bg-danger/10":s==="sent"?"text-accent border-accent/30 bg-accent/10":"text-ink-muted border-border-default bg-surface-2";
export default function QuotationsClient({initial}:{initial:Quotation[]}){const[search,setSearch]=useState("");const[status,setStatus]=useState<QuotationStatus|"all">("all");const rows=useMemo(()=>initial.filter(q=>(status==="all"||q.status===status)&&(!search||`${q.quotationNumber} ${q.customerName} ${q.customerPhone}`.toLowerCase().includes(search.toLowerCase()))),[initial,search,status]);const total=initial.reduce((s,q)=>s+q.grandTotal,0),accepted=initial.filter(q=>q.status==="accepted").reduce((s,q)=>s+q.grandTotal,0);return <div className="space-y-6"><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{[["Total quotations",initial.length],["Pipeline value",money(total)],["Accepted value",money(accepted)],["Conversion rate",`${initial.length?Math.round(initial.filter(q=>q.status==="accepted").length/initial.length*100):0}%`]].map(([a,b])=><div className={`${ui.card} p-5`} key={a}><p className="text-xs uppercase tracking-[.16em] text-ink-faint">{a}</p><p className="mt-2 text-2xl font-semibold">{b}</p></div>)}</div><div className={`${ui.card} p-4`}><div className="flex flex-col gap-3 md:flex-row"><input className={ui.input} placeholder="Search quotation, customer or phone…" value={search} onChange={e=>setSearch(e.target.value)}/><select className={ui.input} value={status} onChange={e=>setStatus(e.target.value as QuotationStatus|"all")}><option value="all">All statuses</option>{Object.entries(QUOTATION_STATUS_LABELS).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select><Link className={`${ui.btnPrimary} shrink-0`} href="/dashboard/quotations/new">+ New quotation</Link></div></div><div className={`${ui.card} overflow-hidden`}><div className="overflow-x-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead className="bg-surface-2 text-xs uppercase tracking-wider text-ink-faint"><tr>{["Quotation","Customer","Service","Value","Valid until","Status",""].map(x=><th className="px-5 py-4" key={x}>{x}</th>)}</tr></thead><tbody className="divide-y divide-border-default">{rows.map(q=><tr key={q._id} className="hover:bg-surface-2/50"><td className="px-5 py-4 font-mono text-accent">{q.quotationNumber}</td><td className="px-5 py-4"><b>{q.customerName}</b><div className="text-xs text-ink-muted">{q.customerPhone}</div></td><td className="px-5 py-4">{SERVICE_TYPE_LABELS[q.serviceType]}</td><td className="px-5 py-4 font-semibold">{money(q.grandTotal)}</td><td className="px-5 py-4">{new Date(q.validUntil).toLocaleDateString("en-IN")}</td><td className="px-5 py-4"><span className={`rounded-full border px-2.5 py-1 text-xs ${badge(q.status)}`}>{QUOTATION_STATUS_LABELS[q.status]}</span></td><td className="px-5 py-4"><Link className={ui.btnGhostSm} href={`/dashboard/quotations/${q._id}`}>Open →</Link></td></tr>)}</tbody></table></div>{!rows.length&&<div className="p-12 text-center text-ink-muted">No quotations match this view.</div>}</div></div>}
