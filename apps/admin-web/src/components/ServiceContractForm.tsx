"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import {ui} from "@/lib/ui-classes";import {ServiceType,SERVICE_TYPE_LABELS} from "@/types/project";import {ContractFrequency,FREQUENCY_LABELS} from "@/types/serviceContract";const today=new Date().toISOString().slice(0,10);const year=(()=>{const d=new Date();d.setFullYear(d.getFullYear()+1);return d.toISOString().slice(0,10)})();type ServiceContractFormState={customerName:string;customerPhone:string;customerEmail:string;address:string;serviceType:ServiceType;frequency:ContractFrequency;customIntervalDays:number;startDate:string;endDate:string;contractValue:number;includedVisits:number;notes:string;terms:string};export default function ServiceContractForm(){const r=useRouter();const[f,setF]=useState<ServiceContractFormState>({customerName:"",customerPhone:"",customerEmail:"",address:"",serviceType:ServiceType.COCKROACH,frequency:"quarterly",customIntervalDays:30,startDate:today,endDate:year,contractValue:0,includedVisits:4,notes:"",terms:"Service visits are subject to site access and Pest Mantra safety procedures. Renewal is required after the contract end date."});const[busy,setBusy]=useState(false);const[err,setErr]=useState("");const set=<K extends keyof ServiceContractFormState>(k:K,v:ServiceContractFormState[K])=>setF(x=>({...x,[k]:v}));async function submit(e:FormEvent){e.preventDefault();setBusy(true);setErr("");const res=await fetch("/api/service-contracts",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(f)});const j=await res.json();setBusy(false);if(!res.ok){setErr(j.message||"Could not create contract");return}r.push(`/dashboard/service-contracts/${j.data.contract._id}`);r.refresh()}return <form onSubmit={submit} className="space-y-5"><div className={`${ui.card} p-6`}><h2 className="text-lg font-semibold">Customer & service</h2><div className="mt-5 grid gap-4 md:grid-cols-2"><input required className={ui.input} placeholder="Customer name" value={f.customerName} onChange={e=>set("customerName",e.target.value)}/><input required className={ui.input} placeholder="10-digit phone" value={f.customerPhone} onChange={e=>set("customerPhone",e.target.value)}/><input className={ui.input} type="email" placeholder="Email (optional)" value={f.customerEmail} onChange={e=>set("customerEmail",e.target.value)}/><select className={ui.input} value={f.serviceType} onChange={e=>set("serviceType",e.target.value as ServiceType)}>{Object.entries(SERVICE_TYPE_LABELS).map(([v,l])=><option value={v} key={v}>{l}</option>)}</select><textarea required className={`${ui.input} md:col-span-2`} rows={3} placeholder="Service address" value={f.address} onChange={e=>set("address",e.target.value)}/></div></div><div className={`${ui.card} p-6`}><h2 className="text-lg font-semibold">Contract schedule</h2><div className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-3"><select className={ui.input} value={f.frequency} onChange={e=>set("frequency",e.target.value as ContractFrequency)}>{Object.entries(FREQUENCY_LABELS).map(([v,l])=><option value={v} key={v}>{l}</option>)}</select>{f.frequency==="custom"&&<input className={ui.input} type="number" min="1" value={f.customIntervalDays} onChange={e=>set("customIntervalDays",Number(e.target.value))} placeholder="Interval days"/>}<input required className={ui.input} type="date" value={f.startDate} onChange={e=>set("startDate",e.target.value)}/><input required className={ui.input} type="date" value={f.endDate} onChange={e=>set("endDate",e.target.value)}/><input required className={ui.input} type="number" min="1" max="100" value={f.includedVisits} onChange={e=>set("includedVisits",Number(e.target.value))} placeholder="Included visits"/><input required className={ui.input} type="number" min="0" value={f.contractValue} onChange={e=>set("contractValue",Number(e.target.value))} placeholder="Contract value ₹"/></div></div><div className={`${ui.card} p-6`}><h2 className="text-lg font-semibold">Commercial notes</h2><div className="mt-5 space-y-4"><textarea className={ui.input} rows={3} placeholder="Internal/service notes" value={f.notes} onChange={e=>set("notes",e.target.value)}/><textarea className={ui.input} rows={5} placeholder="Terms & conditions" value={f.terms} onChange={e=>set("terms",e.target.value)}/></div></div>{err&&<div className="rounded-xl border border-danger/40 p-4 text-danger">{err}</div>}<button disabled={busy} className={ui.btnPrimary}>{busy?"Creating…":"Create AMC contract"}</button></form>}
