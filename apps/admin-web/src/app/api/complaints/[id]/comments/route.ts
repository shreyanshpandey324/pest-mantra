import { NextRequest, NextResponse } from "next/server"; import { backendFetch, BackendApiError } from "@/lib/backend-client"; import { requireAccessToken } from "@/lib/require-token"; import { Complaint } from "@/types/complaint";
export async function POST(req:NextRequest,ctx:{params:Promise<{id:string}>}){const t=requireAccessToken(req);if(t instanceof NextResponse)return t;try{const {id}=await ctx.params;const data=await backendFetch<{complaint:Complaint}>(`/complaints/${id}/comments`,{method:"POST",body:await req.json(),accessToken:t});return NextResponse.json({success:true,data},{status:201});}catch(e){return NextResponse.json({success:false,message:e instanceof BackendApiError?e.message:"Could not add comment"},{status:e instanceof BackendApiError?e.statusCode:502});}}
