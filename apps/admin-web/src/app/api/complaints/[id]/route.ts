import { NextRequest, NextResponse } from "next/server"; import { backendFetch, BackendApiError } from "@/lib/backend-client"; import { requireAccessToken } from "@/lib/require-token"; import { Complaint } from "@/types/complaint";
function fail(e:unknown){return NextResponse.json({success:false,message:e instanceof BackendApiError?e.message:"Complaint update failed"},{status:e instanceof BackendApiError?e.statusCode:502});}
export async function PATCH(req:NextRequest,ctx:{params:Promise<{id:string}>}){const t=requireAccessToken(req);if(t instanceof NextResponse)return t;try{const {id}=await ctx.params;const data=await backendFetch<{complaint:Complaint}>(`/complaints/${id}`,{method:"PATCH",body:await req.json(),accessToken:t});return NextResponse.json({success:true,data});}catch(e){return fail(e);}}
