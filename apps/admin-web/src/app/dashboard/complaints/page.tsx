import { ComingSoonPage } from "@/components/ComingSoonPage";

export default function ComplaintsPage() {
  return (
    <ComingSoonPage
      title="Complaints"
      description="No dedicated complaints API exists yet in the documented backend. Customer bookings and issues are currently tracked through the Projects list — a separate complaints workflow can be wired in here once that endpoint is built."
    />
  );
}
