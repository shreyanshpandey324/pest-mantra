import {cookies} from "next/headers";import Link from "next/link";import {ACCESS_COOKIE_NAME} from "@/lib/session";import {backendFetch} from "@/lib/backend-client";import {ServiceContract} from "@/types/serviceContract";import ServiceContractDetail from "@/components/ServiceContractDetail";import {ui} from "@/lib/ui-classes";export default async function Page({params}:{params:Promise<{id:string}>}){const t=(await cookies()).get(ACCESS_COOKIE_NAME)?.value;const {id}=await params;const c=(await backendFetch<{contract:ServiceContract}>(`/service-contracts/${id}`,{accessToken:t})).contract;return <div className="space-y-6"><div className="flex items-end justify-between gap-4"><div><p className="font-mono text-xs uppercase tracking-[.2em] text-accent">{c.contractNumber}</p><h1 className="mt-1 text-3xl font-semibold">{c.customerName}</h1><p className="mt-2 text-sm text-ink-muted">Recurring service contract and visit lifecycle.</p></div><Link className={ui.btnGhost} href="/dashboard/service-contracts">Back</Link></div><ServiceContractDetail contract={c}/></div>}
