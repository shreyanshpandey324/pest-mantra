import { ComingSoonPage } from "@/components/ComingSoonPage";

export default function SettingsPage() {
  return (
    <ComingSoonPage
      title="Settings"
      description="Account, branch, and notification settings don't have a backend endpoint yet. This page is ready to wire up as soon as a settings API exists."
    />
  );
}
