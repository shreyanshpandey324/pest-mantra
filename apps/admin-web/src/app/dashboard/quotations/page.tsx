import{cookies}from"next/headers";import{ACCESS_COOKIE_NAME}from"@/lib/session";import{backendFetch,BackendApiError}from"@/lib/backend-client";import{Quotation}from"@/types/quotation";import QuotationsClient from"@/components/QuotationsClient";
export default async function Page(){const t=(await cookies()).get(ACCESS_COOKIE_NAME)?.value;let q:Quotation[]=[];let error="";try{q=(await backendFetch<{quotations:Quotation[]}>("/quotations",{accessToken:t})).quotations}catch(e){error=e instanceof BackendApiError?e.message:"Could not load quotations"}return <div className="space-y-6"><div className="flex items-end justify-between"><div><p className="font-mono text-xs uppercase tracking-[.2em] text-accent">Sales workspace</p><h1 className="mt-1 text-3xl font-semibold">Quotations</h1><p className="mt-2 text-sm text-ink-muted">Build, send and convert professional pest-control proposals.</p></div></div>{error&&<div className="rounded-xl border border-danger/40 p-4 text-danger">{error}</div>}<QuotationsClient initial={q}/></div>}
